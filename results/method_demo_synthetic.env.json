{
 "note": "Dependency versions in which results/method_demo_synthetic.png was found NOT to reproduce. These are NOT the versions that produced the committed figure -- those are unknown, because no environment record was committed with it. Recorded so the mismatch is reproducible.",
 "committed_figure_reproduces_here": false,
 "observed_in": {
  "python": "3.11.16",
  "numpy": "2.4.6",
  "scipy": "1.17.1",
  "matplotlib": "3.10.1"
 },
 "matplotlib_ruled_out": "committed PNG metadata reports Matplotlib 3.10.1, identical to the version above, at the same canvas (1612x832) and dpi",
 "difference": {
  "fraction_of_pixels_differing": 0.0159,
  "connected_components": 491,
  "components_over_200px": 8,
  "interpretation": "solid-region content change, not antialiasing"
 },
 "generator_is_deterministic": true,
 "evidence_code_did_not_change": "figure, demo script, extractor and synthetic scene all landed in commit 6c94f16; nothing has touched analysis/ since",
 "cause_not_identified": "analysis/pyproject.toml pins only numpy>=1.24 and scipy>=1.10 with no upper bounds; scipy.ndimage or numpy is the likely source but this was not narrowed further and is not asserted"
}
